import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#085726",
    },
    secondary: {
      main: "#9c27b0",
    },
    background: {
      default: '#c8e6c9',
    },
  },
});

export default theme;